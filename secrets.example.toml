"""
Configuration pour Streamlit Cloud Deployment
Ce fichier montre la structure des variables nécessaires
"""

# Optionnel : Ces variables peuvent être ajoutées dans les secrets de Streamlit Cloud
# Console Streamlit → Settings → Secrets → Ajouter

EXAMPLE_CONFIG = {
    # Email de contact (optionnel)
    "CONTACT_EMAIL": "contact@example.com",
    
    # Google Analytics (optionnel) 
    "GA_ID": "",
    
    # API endpoints (si besoin d'appels API externes)
    "API_BASE_URL": "",
}

# Note: Aucune secret sensible n'est requise pour cette application
# Elle fonctionne entièrement en local/client-side
